# Backend API (Express). Omit to default to http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000

# Consultation + summary mocks: true = always on, false = always off, unset = on in `next dev` only.
# NEXT_PUBLIC_USE_API_MOCKS=false
